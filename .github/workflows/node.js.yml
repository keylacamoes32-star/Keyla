📦 Estrutura de Pastas
kc-assistente/
│
├── config/
│   └── settings.json
│
├── database/
│   └── clients.db
│
├── src/
│   ├── bot.js
│   ├── menu.js
│   ├── responses.js
│   └── scheduler.js
│
├── package.json
└── README.md

⚙️ Arquivo config/settings.json
{
  "botName": "KC Assistente",
  "ownerNumber": "879632760",
  "welcomeMessage": "👋 Olá! Bem-vindo(a) à KC Assistente. Como posso ajudar hoje?",
  "timezone": "Africa/Maputo"
}

📜 Arquivo src/menu.js
module.exports = {
  inicial: () => {
    return `
👋 *Bem-vindo(a) à KC Assistente!*
Escolha uma opção abaixo:

1️⃣ Produtos
2️⃣ Serviços
3️⃣ Pagamentos
4️⃣ Falar com humano
`;
  }
};

💬 Arquivo src/responses.js
module.exports = {
  produtos: () => `
🛍️ *Lista de Produtos KC Assistente:*
- Catálogo Digital
- Automação de WhatsApp
- Criação de Chatbots
- Consultoria Técnica
`,

  servicos: () => `
💼 *Serviços KC Assistente:*
- Desenvolvimento de Bots
- Gestão de Atendimento
- Marketing Digital
- Suporte Técnico
`,

  pagamentos: () => `
💳 *Formas de Pagamento:*
- e-Mola: 879632760
- M-Pesa: 879632760
- Transferência Bancária (solicite dados)
Após o pagamento, envie o comprovativo para confirmação.
`
};

🤖 Arquivo src/bot.js
const { makeWASocket, useMultiFileAuthState } = require('@whiskeysockets/baileys');
const fs = require('fs');
const sqlite3 = require('sqlite3').verbose();
const menu = require('./menu');
const responses = require('./responses');
const settings = require('../config/settings.json');

(async () => {
  const { state, saveCreds } = await useMultiFileAuthState('auth');
  const sock = makeWASocket({ auth: state });

  const db = new sqlite3.Database('./database/clients.db');
  db.run('CREATE TABLE IF NOT EXISTS clients (id INTEGER PRIMARY KEY, name TEXT, number TEXT, history TEXT)');

  sock.ev.on('messages.upsert', async ({ messages }) => {
    const msg = messages[0];
    if (!msg.message) return;
    const from = msg.key.remoteJid;
    const text = msg.message.conversation || msg.message.extendedTextMessage?.text;

    if (!text) return;

    console.log(`Mensagem recebida de ${from}: ${text}`);

    if (text === '1') await sock.sendMessage(from, { text: responses.produtos() });
    else if (text === '2') await sock.sendMessage(from, { text: responses.servicos() });
    else if (text === '3') await sock.sendMessage(from, { text: responses.pagamentos() });
    else if (text === '4') await sock.sendMessage(from, { text: '👩🏽‍💼 Um atendente humano irá responder em breve.' });
    else await sock.sendMessage(from, { text: menu.inicial() });
  });

  sock.ev.on('creds.update', saveCreds);

  console.log(`🤖 ${settings.botName} está online e pronto para atender!`);
})();

🗄️ Banco de Dados Local (SQLite)
O banco clients.db é criado automaticamente e guarda:

Nome do cliente
Número de telefone
Histórico de conversas
🚀 Instalação
Instalar Node.js (v18 ou superior).
Criar a pasta do projeto e copiar os arquivos acima.
No terminal, executar:
npm init -y
npm install @whiskeysockets/baileys sqlite3
Iniciar o bot:
node src/bot.js
Escanear o QR Code com o WhatsApp Business.
🔁 Execução Contínua (24/24)
Para manter o bot sempre ativo:

npm install -g pm2
pm2 start src/bot.js --name kc-assistente
pm2 save
pm2 startup

🔒 Segurança e Controlo
Uso exclusivo do proprietário.
Dados armazenados localmente.
Sem dependência de plataformas pagas.
Código 100% open-source e personalizável.
📞 Contato
Marca: KC Assistente
Telefone: 879632760
Desenvolvido por: Canva AI
